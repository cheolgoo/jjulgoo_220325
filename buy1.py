# buy1
import numpy as np
import pyupbit

if ( count >= n_count ) or ( delta < -tic * bigone ) :

    # 10% buy operation
    upbit.buy_market_order("KRW-BTG", balance * 0.1)
    buy_price[0]    = pyupbit.get_current_price("KRW-BTG")
    buy_vol[0]      = balance * 0.1 
    buy_count[0]    = 1 
    buy_num[0]      = 1 

    
    
    count           = 0 
